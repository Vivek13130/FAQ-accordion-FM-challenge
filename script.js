const plus_icons = document.querySelectorAll(".mainque img")
console.log(plus_icons)

// assigning an event listener for all + images :

plus_icons.forEach((plus) => {
    plus.addEventListener("click", () => {
        plus.
    })
})